# Задачи: Исправление лишних оберток в Hero компоненте

## 1. Изменить логику рендеринга в default.cshtml
- [x] 1.1 Открыть файл `TestUmbraco/Views/Partials/blockgrid/default.cshtml`
- [x] 1.2 Извлечь `item.Content.ContentType.Alias` в переменную `alias`
- [x] 1.3 Добавить `hero` в условие проверки полноширинных компонентов
- [x] 1.4 Использовать переменную `alias` во всех местах вместо повторного обращения

**Детали:**
Изменить условие с:
```csharp
if (item.Content.ContentType.Alias == "gridSection")
```

На:
```csharp
var alias = item.Content.ContentType.Alias;
if (alias == "gridSection" || alias == "hero")
```

## 2. Проверить результат в браузере
- [x] 2.1 Запустить приложение
- [x] 2.2 Открыть страницу с Hero компонентом
- [x] 2.3 Открыть DevTools и проверить структуру DOM
- [x] 2.4 Убедиться, что `<section>` является корневым элементом без оберток `container/row/col`
- [x] 2.5 Убедиться, что внутри `<section>` есть `<div class="container"><div class="hero-content">`

**Ожидаемая структура:**
```html
<section class="text-inverse-... bg-media-... lazy-bg overlay-...">
  <div class="container">
    <div class="hero-content">
      <!-- Контент -->
    </div>
  </div>
</section>
```

## 3. Проверить визуальное отображение
- [x] 3.1 Проверить, что Hero компонент отображается корректно
- [x] 3.2 Проверить применение фоновых изображений/видео
- [x] 3.3 Проверить работу overlay эффектов
- [x] 3.4 Проверить применение textInverse стилей
- [x] 3.5 Проверить responsive поведение на разных разрешениях

## 4. Проверить другие компоненты
- [x] 4.1 Открыть страницу с обычными блоками (не hero, не gridSection)
- [x] 4.2 Убедиться, что они по-прежнему оборачиваются в `container/row/col`
- [x] 4.3 Проверить работу gridSection компонента
- [x] 4.4 Убедиться, что ColumnSpan работает для обычных блоков

## 5. Написать unit тесты (опционально)
- [ ]* 5.1 Создать тест для проверки отсутствия оберток у hero
- [ ]* 5.2 Создать тест для проверки наличия оберток у обычных компонентов
- [ ]* 5.3 Создать тест для проверки структуры контента hero

**Примечание:** Тесты опциональны, так как это простое изменение, которое легко проверить вручную.

## 6. Документировать изменения
- [ ] 6.1 Добавить комментарий в код о полноширинных компонентах
- [ ] 6.2 Обновить документацию проекта (если есть)

**Комментарий для кода:**
```csharp
@* Полноширинные компоненты (gridSection, hero) рендерятся напрямую без оберток Bootstrap grid *@
```

## Критерии завершения

- ✅ Hero компонент рендерится без лишних оберток `container/row/col`
- ✅ Семантический тег `<section>` является корневым элементом
- ✅ Все стили и фоны применяются корректно
- ✅ Другие компоненты работают без изменений
- ✅ Нет визуальных регрессий
- ✅ Код читаемый и содержит комментарии

## Оценка времени

- Задача 1: 5 минут
- Задача 2: 5 минут
- Задача 3: 10 минут
- Задача 4: 10 минут
- Задача 5: 30 минут (опционально)
- Задача 6: 5 минут

**Общее время:** ~35 минут (без опциональных тестов)
