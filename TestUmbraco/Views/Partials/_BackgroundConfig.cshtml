@using TestUmbraco.Services
@using System.Collections.Generic

@{
    // Этот файл остается для совместимости, но теперь стили генерируются в статический CSS
    // Поэтому он может быть пустым или содержать только ссылку на CSS файл
    
    var version = DateTime.Now.ToString("yyyyMMdd");
    var cssLink = $"/css/backgrounds.css?v={version}";
}

<!-- Ссылка на статический CSS файл с фонами -->
<link rel="stylesheet" href="@cssLink" />

@*
    Старая логика инлайн-стилей больше не используется.
    Все стили теперь генерируются в backgrounds.css
*@