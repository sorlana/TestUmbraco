# Конструктор форм для Umbraco CMS

## Обзор

**Конструктор форм** - это модульное расширение для Umbraco CMS, которое позволяет редакторам контента создавать и управлять динамическими формами обратной связи через бэкофис без необходимости программирования. Система интегрируется с существующей инфраструктурой проекта, использует MailKit для отправки email, reCAPTCHA для защиты от спама и сохраняет все отправки в базу данных для последующего анализа.

### Основные возможности

✅ **Визуальный конструктор форм** - создание форм через бэкофис Umbraco без кода  
✅ **Гибкая валидация** - встроенная валидация + кастомные регулярные выражения  
✅ **Защита от спама** - интеграция с Google reCAPTCHA v3  
✅ **Email уведомления** - автоматическая отправка писем через MailKit  
✅ **Сохранение в БД** - все отправки сохраняются для анализа  
✅ **Адаптивный дизайн** - формы автоматически адаптируются под мобильные устройства  
✅ **Client & Server валидация** - двойная защита от некорректных данных  
✅ **Множество типов полей** - text, email, phone, textarea, checkbox, radio, select  

### Версия

**Текущая версия:** 1.0  
**Дата релиза:** 10.02.2025  
**Совместимость:** Umbraco CMS 10+, .NET 8.0+

---

## Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Установка](#установка)
3. [Конфигурация](#конфигурация)
4. [Создание форм](#создание-форм)
5. [Архитектура](#архитектура)
6. [Документация](#документация)
7. [Устранение неполадок](#устранение-неполадок)
8. [Поддержка и вклад](#поддержка-и-вклад)

---

## Быстрый старт

### Шаг 1: Настройка Document Types

Создайте два Document Type в бэкофисе Umbraco:
- `formField` - определяет структуру одного поля формы
- `formBuilderBlock` - определяет полную конфигурацию формы

📖 **Подробная инструкция:** [FORM_BUILDER_SETUP_GUIDE.md](FORM_BUILDER_SETUP_GUIDE.md)


### Шаг 2: Настройка конфигурации

Добавьте настройки в `appsettings.json`:

```json
{
  "ReCaptcha": {
    "SiteKey": "ваш-site-key-от-google",
    "SecretKey": "ваш-secret-key-от-google"
  },
  "SmtpSettings": {
    "Host": "smtp.example.com",
    "Port": 587,
    "EnableSsl": true,
    "Username": "your-email@example.com",
    "Password": "your-password",
    "FromEmail": "noreply@example.com",
    "FromName": "Ваш сайт"
  }
}
```

### Шаг 3: Создайте первую форму

1. Перейдите в раздел **Content** в бэкофисе
2. Добавьте блок `formBuilderBlock` на страницу
3. Заполните настройки формы:
   - **Form Title:** "Свяжитесь с нами"
   - **Form Fields:** добавьте поля (Имя, Email, Сообщение)
   - **Email Recipient:** ваш email
   - **Email Subject:** "Новое сообщение с сайта"
4. Сохраните и опубликуйте

🎉 **Готово!** Форма готова к использованию.

📖 **Примеры форм:** [FORM_BUILDER_EXAMPLES.md](FORM_BUILDER_EXAMPLES.md)

---

## Установка

### Предварительные требования

- Umbraco CMS 10+ установлена и запущена
- .NET 8.0 SDK или выше
- SQL Server или другая поддерживаемая БД
- Доступ к SMTP серверу для отправки email
- Google reCAPTCHA ключи (бесплатно на [google.com/recaptcha](https://www.google.com/recaptcha/admin))

### Шаг 1: Проверка структуры проекта

Убедитесь, что проект имеет следующую структуру:

```
Solution/
├── TestUmbraco/                    # Presentation Layer
│   ├── Controllers/
│   │   └── FormBuilderController.cs
│   ├── Models/
│   │   └── FormSubmissionModel.cs
│   ├── Views/
│   │   └── Partials/
│   │       └── FormBuilderBlock.cshtml
│   └── wwwroot/
│       └── js/
│           └── form-builder.js
│
├── TestUmbraco.Application/        # Application Layer
│   ├── Contracts/
│   │   ├── IEmailService.cs
│   │   └── IFormSubmissionService.cs
│   ├── DTO/
│   │   ├── FormFieldDto.cs
│   │   └── FormSubmissionDto.cs
│   └── Services/
│       ├── EmailService.cs
│       └── FormSubmissionService.cs
│
└── TestUmbraco.Domain/             # Domain Layer
    ├── Contracts/
    │   └── IFormSubmissionRepository.cs
    ├── Models/
    │   └── FormSubmission.cs
    ├── Repositories/
    │   └── FormSubmissionRepository.cs
    └── Migrations/
        └── 20260210120000_AddFormSubmissionsTable.cs
```

### Шаг 2: Применение миграций базы данных

Выполните миграцию для создания таблицы `FormSubmissions`:

```bash
cd TestUmbraco.Domain
dotnet ef database update
```

Или выполните SQL скрипт вручную:

```sql
CREATE TABLE FormSubmissions (
    Id INT PRIMARY KEY IDENTITY(1,1),
    FormId INT NOT NULL,
    FormTitle NVARCHAR(255) NOT NULL,
    SubmittedAt DATETIME NOT NULL,
    IpAddress NVARCHAR(50),
    FieldValuesJson NVARCHAR(MAX) NOT NULL,
    CreatedAt DATETIME NOT NULL DEFAULT GETUTCDATE(),
    INDEX IX_FormSubmissions_FormId (FormId),
    INDEX IX_FormSubmissions_SubmittedAt (SubmittedAt)
);
```

### Шаг 3: Регистрация сервисов

Сервисы уже зарегистрированы в DI контейнере через Composer. Проверьте наличие регистраций в `Startup.cs` или `Composer`:

```csharp
services.AddScoped<IFormSubmissionRepository, FormSubmissionRepository>();
services.AddScoped<IFormSubmissionService, FormSubmissionService>();
services.AddScoped<IEmailService, EmailService>();
```

### Шаг 4: Настройка Document Types

Следуйте подробной инструкции в [FORM_BUILDER_SETUP_GUIDE.md](FORM_BUILDER_SETUP_GUIDE.md) для создания:
- Document Type `formField`
- Document Type `formBuilderBlock`

---

## Конфигурация

### Email настройки (SMTP)

Настройте SMTP для отправки email уведомлений в `appsettings.json`:

```json
{
  "SmtpSettings": {
    "Host": "smtp.gmail.com",           // SMTP сервер
    "Port": 587,                        // Порт (обычно 587 или 465)
    "EnableSsl": true,                  // Использовать SSL/TLS
    "Username": "your-email@gmail.com", // Логин
    "Password": "your-app-password",    // Пароль или App Password
    "FromEmail": "noreply@yoursite.com",// Email отправителя
    "FromName": "Ваш сайт"              // Имя отправителя
  }
}
```

**Примеры настроек для популярных провайдеров:**

| Провайдер | Host | Port | SSL |
|-----------|------|------|-----|
| Gmail | smtp.gmail.com | 587 | true |
| Outlook | smtp-mail.outlook.com | 587 | true |
| Yandex | smtp.yandex.ru | 465 | true |
| Mail.ru | smtp.mail.ru | 465 | true |

⚠️ **Важно:** Для Gmail используйте [App Passwords](https://support.google.com/accounts/answer/185833), а не обычный пароль.


### Google reCAPTCHA настройки

1. **Получите ключи reCAPTCHA:**
   - Перейдите на [google.com/recaptcha/admin](https://www.google.com/recaptcha/admin)
   - Зарегистрируйте новый сайт
   - Выберите **reCAPTCHA v3** (рекомендуется)
   - Добавьте домены вашего сайта
   - Скопируйте **Site Key** и **Secret Key**

2. **Добавьте ключи в конфигурацию:**

```json
{
  "ReCaptcha": {
    "SiteKey": "6LcXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
    "SecretKey": "6LcYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYY"
  }
}
```

3. **Для разработки (localhost):**
   - reCAPTCHA v3 работает на localhost без дополнительных настроек
   - Добавьте `localhost` в список доменов в консоли reCAPTCHA

### База данных

Модуль использует Entity Framework Core для работы с БД. Настройки подключения находятся в `appsettings.json`:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=UmbracoDb;Trusted_Connection=True;TrustServerCertificate=True;"
  }
}
```

**Поддерживаемые БД:**
- SQL Server (рекомендуется)
- SQLite (для разработки)
- PostgreSQL
- MySQL/MariaDB

---

## Создание форм

### Создание формы в бэкофисе

#### 1. Базовая настройка формы

1. Откройте страницу в разделе **Content**
2. Добавьте блок **formBuilderBlock**
3. Заполните вкладку **Form Settings:**

| Поле | Описание | Обязательное |
|------|----------|--------------|
| **Form Title** | Заголовок формы | ✓ |
| **Form Description** | Описание под заголовком | ☐ |
| **Form Fields** | Список полей формы | ✓ |
| **Submit Button Text** | Текст кнопки (по умолчанию "Отправить") | ☐ |

#### 2. Добавление полей

Нажмите **Add content** в **Form Fields** и настройте каждое поле:

| Свойство | Описание | Пример |
|----------|----------|--------|
| **Field Label** | Надпись над полем | "Ваше имя" |
| **Field Type** | Тип поля | text, email, phone, textarea, checkbox, radio, select |
| **Field Placeholder** | Подсказка внутри поля | "Введите ваше имя" |
| **Is Required** | Обязательное поле | ✓ или ☐ |
| **Validation Pattern** | Regex для валидации | `^\d{10}$` (10 цифр) |
| **Error Message** | Сообщение об ошибке | "Введите корректный номер" |

#### 3. Настройка Email

Заполните вкладку **Email Settings:**

| Поле | Описание | Обязательное |
|------|----------|--------------|
| **Email Recipient** | Email получателя | ✓ |
| **Email Subject** | Тема письма | ✓ |
| **Success Message** | Сообщение после отправки | ☐ |
| **Redirect Page** | Страница для редиректа | ☐ |

### Типы полей

| Тип | HTML элемент | Использование |
|-----|--------------|---------------|
| **text** | `<input type="text">` | Однострочный текст (имя, город) |
| **email** | `<input type="email">` | Email адрес с валидацией |
| **phone** | `<input type="tel">` | Номер телефона |
| **textarea** | `<textarea>` | Многострочный текст (сообщение, комментарий) |
| **checkbox** | `<input type="checkbox">` | Согласие, подтверждение |
| **radio** | `<input type="radio">` | Выбор одного варианта |
| **select** | `<select>` | Выпадающий список |

### Примеры регулярных выражений

**Телефон (российский формат):**
```regex
^\+?7[\s\-]?\(?[0-9]{3}\)?[\s\-]?[0-9]{3}[\s\-]?[0-9]{2}[\s\-]?[0-9]{2}$
```

**Только буквы (кириллица):**
```regex
^[А-Яа-яЁё\s\-]{2,50}$
```

**Дата (ДД.ММ.ГГГГ):**
```regex
^(0[1-9]|[12][0-9]|3[01])\.(0[1-9]|1[012])\.(19|20)\d\d$
```

**Почтовый индекс (6 цифр):**
```regex
^\d{6}$
```

📖 **Больше примеров:** [FORM_BUILDER_EXAMPLES.md](FORM_BUILDER_EXAMPLES.md)

---

## Архитектура

### Обзор архитектуры

Модуль построен на принципах **Clean Architecture** с разделением на слои:

```
┌─────────────────────────────────────────────────────────┐
│                  Presentation Layer                     │
│  (Controllers, Views, JavaScript)                       │
│  - FormBuilderController.cs                            │
│  - FormBuilderBlock.cshtml                             │
│  - form-builder.js                                     │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                  Application Layer                      │
│  (Services, DTOs, Business Logic)                       │
│  - IEmailService / EmailService                         │
│  - IFormSubmissionService / FormSubmissionService       │
│  - FormSubmissionDto, FormFieldDto                      │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                    Domain Layer                         │
│  (Entities, Repositories, Interfaces)                   │
│  - FormSubmission (Entity)                              │
│  - IFormSubmissionRepository                            │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                 Infrastructure Layer                    │
│  (Database, External Services)                          │
│  - FormSubmissionRepository                             │
│  - Entity Framework Core                                │
│  - MailKit (SMTP)                                       │
│  - Google reCAPTCHA API                                 │
└─────────────────────────────────────────────────────────┘
```

### Поток обработки формы

```
1. Посетитель заполняет форму на фронтенде
   ↓
2. Client-side валидация (JavaScript)
   ↓
3. Получение reCAPTCHA токена
   ↓
4. AJAX отправка на FormBuilderController
   ↓
5. Server-side валидация
   ↓
6. Проверка reCAPTCHA токена
   ↓
7. Создание FormSubmissionDto
   ↓
8. Сохранение в БД через FormSubmissionService
   ↓
9. Отправка email через EmailService
   ↓
10. Возврат результата (Success/Error)
    ↓
11. Показ Toast notification или редирект
```

### Ключевые компоненты

#### FormBuilderController (SurfaceController)

Обрабатывает POST запросы от форм:
- Проверяет reCAPTCHA токен
- Выполняет server-side валидацию
- Координирует сохранение в БД и отправку email
- Возвращает JSON результат

#### FormSubmissionService

Бизнес-логика обработки отправок:
- Преобразует DTO в Entity
- Сохраняет в БД через Repository
- Обрабатывает ошибки с логированием

#### EmailService

Отправка email уведомлений:
- Использует MailKit для SMTP
- Форматирует данные формы в HTML
- Поддерживает настройку через конфигурацию

#### FormSubmissionRepository

Доступ к данным:
- Использует Entity Framework Core
- Сохраняет отправки в таблицу FormSubmissions
- Предоставляет методы для получения истории отправок


### Модель данных

#### FormSubmission Entity

```csharp
public class FormSubmission
{
    public int Id { get; set; }
    public int FormId { get; set; }              // ID формы в Umbraco
    public string FormTitle { get; set; }        // Название формы
    public DateTime SubmittedAt { get; set; }    // Дата и время отправки
    public string IpAddress { get; set; }        // IP адрес отправителя
    public string FieldValuesJson { get; set; }  // JSON с данными полей
    public DateTime CreatedAt { get; set; }      // Дата создания записи
}
```

#### FormSubmissionDto

```csharp
public class FormSubmissionDto
{
    public int FormId { get; set; }
    public string FormTitle { get; set; }
    public DateTime SubmittedAt { get; set; }
    public string IpAddress { get; set; }
    public Dictionary<string, string> FieldValues { get; set; }
}
```

### Валидация

Модуль использует **двойную валидацию** для максимальной защиты:

#### Client-side валидация (JavaScript)

- ✅ Проверка обязательных полей
- ✅ Валидация по регулярным выражениям
- ✅ Валидация email и телефона
- ✅ Real-time обратная связь
- ✅ Предотвращение отправки невалидной формы

#### Server-side валидация (C#)

- ✅ Повторная проверка всех правил
- ✅ Защита от обхода client-side валидации
- ✅ Проверка reCAPTCHA токена
- ✅ Валидация ModelState
- ✅ Обработка ошибок с логированием

---

## Документация

### Основные документы

| Документ | Описание |
|----------|----------|
| **README.md** (этот файл) | Общий обзор и руководство по модулю |
| [FORM_BUILDER_SETUP_GUIDE.md](FORM_BUILDER_SETUP_GUIDE.md) | Подробная инструкция по настройке Document Types |
| [FORM_BUILDER_EXAMPLES.md](FORM_BUILDER_EXAMPLES.md) | Практические примеры создания форм |

### Дополнительные ресурсы

- [Официальная документация Umbraco CMS](https://docs.umbraco.com/)
- [Документация MailKit](https://github.com/jstedfast/MailKit)
- [Google reCAPTCHA документация](https://developers.google.com/recaptcha/docs/v3)
- [Regex тестер](https://regex101.com/) - для проверки регулярных выражений

### API Reference

#### IFormSubmissionService

```csharp
public interface IFormSubmissionService
{
    Task<bool> ProcessSubmissionAsync(FormSubmissionDto dto);
    Task<IEnumerable<FormSubmission>> GetSubmissionsByFormAsync(int formId);
}
```

#### IEmailService

```csharp
public interface IEmailService
{
    Task<bool> SendFormSubmissionAsync(
        FormSubmissionDto submission,
        string recipientEmail,
        string subject
    );
}
```

#### IFormSubmissionRepository

```csharp
public interface IFormSubmissionRepository
{
    Task<int> SaveAsync(FormSubmission submission);
    Task<FormSubmission> GetByIdAsync(int id);
    Task<IEnumerable<FormSubmission>> GetByFormIdAsync(int formId);
    Task<IEnumerable<FormSubmission>> GetAllAsync(int pageNumber, int pageSize);
}
```

---

## Устранение неполадок

### Форма не отображается на странице

**Возможные причины:**
1. Document Types не созданы или настроены неправильно
2. Представление `FormBuilderBlock.cshtml` отсутствует
3. JavaScript файл `form-builder.js` не подключен

**Решение:**
- Проверьте наличие Document Types в Settings → Document Types
- Убедитесь, что файл `Views/Partials/FormBuilderBlock.cshtml` существует
- Проверьте консоль браузера (F12) на наличие ошибок JavaScript

### Форма не отправляется

**Возможные причины:**
1. Ошибка валидации полей
2. reCAPTCHA не настроена или токен невалиден
3. Ошибка на сервере (проверьте логи)

**Решение:**
- Откройте консоль браузера (F12) и проверьте ошибки
- Убедитесь, что все обязательные поля заполнены
- Проверьте настройки reCAPTCHA в `appsettings.json`
- Проверьте логи Umbraco в папке `Logs/`

### Email не отправляется

**Возможные причины:**
1. Неправильные SMTP настройки
2. Блокировка SMTP сервером (firewall, антивирус)
3. Неправильный логин/пароль

**Решение:**
- Проверьте настройки SMTP в `appsettings.json`
- Для Gmail используйте App Password, а не обычный пароль
- Проверьте логи на наличие ошибок SMTP
- Попробуйте отправить тестовое письмо через EmailController

**Тестирование SMTP:**
```bash
# Проверьте доступность SMTP сервера
telnet smtp.gmail.com 587
```

### Ошибка "reCAPTCHA verification failed"

**Возможные причины:**
1. Неправильные ключи reCAPTCHA
2. Домен не добавлен в настройки reCAPTCHA
3. Использование reCAPTCHA v2 вместо v3

**Решение:**
- Проверьте Site Key и Secret Key в `appsettings.json`
- Убедитесь, что домен добавлен в консоли reCAPTCHA
- Используйте reCAPTCHA v3 (invisible)
- Для localhost добавьте его в список доменов

### Ошибка базы данных

**Возможные причины:**
1. Миграция не применена
2. Таблица FormSubmissions не существует
3. Неправильная строка подключения

**Решение:**
```bash
# Применить миграции
cd TestUmbraco.Domain
dotnet ef database update

# Проверить подключение к БД
dotnet ef database update --verbose
```

### Валидация не работает

**Client-side валидация:**
- Проверьте, что `form-builder.js` загружен
- Откройте консоль браузера на наличие ошибок JavaScript
- Убедитесь, что data-атрибуты установлены на полях

**Server-side валидация:**
- Проверьте логи на наличие ошибок валидации
- Убедитесь, что ModelState проверяется в контроллере

### Проблемы с регулярными выражениями

**Симптомы:**
- Валидация не срабатывает
- Ошибка "Invalid regular expression"

**Решение:**
- Проверьте regex на [regex101.com](https://regex101.com/)
- Убедитесь, что специальные символы экранированы
- Используйте простые паттерны для начала

### Отладка

**Включение подробного логирования:**

В `appsettings.json`:
```json
{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft": "Warning",
        "System": "Warning"
      }
    }
  }
}
```

**Проверка логов:**
```bash
# Логи Umbraco
tail -f TestUmbraco/Logs/log-YYYYMMDD.txt

# Логи приложения
tail -f TestUmbraco/umbraco/Logs/UmbracoTraceLog.YYYYMMDD.json
```

---

## Поддержка и вклад

### Получение помощи

Если у вас возникли проблемы или вопросы:

1. **Проверьте документацию:**
   - [FORM_BUILDER_SETUP_GUIDE.md](FORM_BUILDER_SETUP_GUIDE.md)
   - [FORM_BUILDER_EXAMPLES.md](FORM_BUILDER_EXAMPLES.md)
   - Раздел [Устранение неполадок](#устранение-неполадок)

2. **Проверьте логи:**
   - `TestUmbraco/Logs/` - логи приложения
   - Консоль браузера (F12) - ошибки JavaScript

3. **Свяжитесь с командой разработки:**
   - Email: support@yourcompany.com
   - Issue Tracker: (если используется)

### Сообщение об ошибках

При сообщении об ошибке укажите:
- Версию Umbraco CMS
- Версию .NET
- Описание проблемы
- Шаги для воспроизведения
- Сообщения об ошибках из логов
- Скриншоты (если применимо)

### Вклад в проект

Мы приветствуем вклад в развитие модуля! Если вы хотите:
- Исправить ошибку
- Добавить новую функцию
- Улучшить документацию

Пожалуйста, следуйте этим шагам:
1. Создайте ветку для вашей функции
2. Напишите тесты для новой функциональности
3. Убедитесь, что все тесты проходят
4. Создайте Pull Request с описанием изменений

### Roadmap (Планы развития)

**Версия 1.1 (планируется):**
- ✨ Загрузка файлов через формы
- ✨ Условная логика (показ/скрытие полей)
- ✨ Интеграция с CRM системами
- ✨ Экспорт отправок в Excel/CSV
- ✨ Дашборд в бэкофисе для просмотра отправок

**Версия 1.2 (планируется):**
- ✨ Multi-step формы (многошаговые)
- ✨ Автоответы пользователям
- ✨ Интеграция с платежными системами
- ✨ A/B тестирование форм
- ✨ Расширенная аналитика

### Лицензия

Этот модуль разработан для внутреннего использования в проекте TestUmbraco.

---

## Контрольный список

Используйте этот список для проверки правильности установки:

### Установка
- [ ] Document Type `formField` создан и настроен
- [ ] Document Type `formBuilderBlock` создан и настроен
- [ ] Миграция базы данных применена
- [ ] Таблица `FormSubmissions` существует в БД

### Конфигурация
- [ ] SMTP настройки добавлены в `appsettings.json`
- [ ] reCAPTCHA ключи добавлены в `appsettings.json`
- [ ] Строка подключения к БД настроена
- [ ] Сервисы зарегистрированы в DI контейнере

### Тестирование
- [ ] Создана тестовая форма в бэкофисе
- [ ] Форма отображается на фронтенде
- [ ] Client-side валидация работает
- [ ] Форма успешно отправляется
- [ ] Email приходит на указанный адрес
- [ ] Отправка сохраняется в БД
- [ ] Toast notification показывается после отправки

---

## Благодарности

Модуль использует следующие библиотеки и сервисы:
- **Umbraco CMS** - платформа управления контентом
- **MailKit** - библиотека для отправки email
- **Entity Framework Core** - ORM для работы с БД
- **Google reCAPTCHA** - защита от спама
- **Bootstrap** - CSS фреймворк (опционально)

---

**Версия документа:** 1.0  
**Дата обновления:** 10.02.2025  
**Автор:** Команда разработки TestUmbraco

**Удачи в создании форм! 🚀**
